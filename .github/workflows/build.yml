name: 'CI: build'

on:
  push: {}

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
  
  sub1:
    strategy:
      matrix:
        name: ['foo', 'bar']
    uses: "./.github/workflows/sub-${{ vars.name }}.yml"
