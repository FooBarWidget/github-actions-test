name: 'CI: build'

on:
  push: {}
  pull_request_target:
    types: [synchronize]

jobs:
  foo:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - run: echo v1
      - run: cat README.md
