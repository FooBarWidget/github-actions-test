name: 'ci_test'

on:
  workflow_dispatch:
    inputs:
      parent_run_number:
        required: false
  workflow_run:
    workflows:
      - 'ci_build'
    #types:
    #  - completed

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - run: echo "$EVENT"
        env:
          EVENT: ${{ toJSON(github.event) }}
